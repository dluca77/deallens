# Vereist voor echte productherkenning via /api/analyze.
# Lokaal: kopieer dit bestand naar .env.local en vul je sleutel in.
# Op Cloudflare: stel in met `npx wrangler secret put ANTHROPIC_API_KEY`
# of via het dashboard onder Settings > Variables and Secrets.
ANTHROPIC_API_KEY=
